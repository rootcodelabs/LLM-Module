{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: liquibase-secrets
  labels:
    app: "{{ .Values.release_name }}"
type: Opaque
stringData:
  POSTGRES_USER: "<SET_ME>"
  POSTGRES_PASSWORD: "<SET_ME>"
{{- end }}
