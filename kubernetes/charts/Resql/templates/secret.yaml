{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: resql-secrets
  labels:
    app: "{{ .Values.release_name }}"
type: Opaque
stringData:
  SQLMS_DATASOURCES_0_PASSWORD: "<SET_ME>"
{{- end }}
