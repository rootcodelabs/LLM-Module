{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: ruuter-private-secrets
  labels:
    app: "{{ .Values.release_name }}"
type: Opaque
stringData:
  DB_PASSWORD: "<SET_ME>"
{{- end }}
