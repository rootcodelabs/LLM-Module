{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: redis-secrets
  labels:
    app: "{{ .Values.release_name }}"
    component: redis
type: Opaque
stringData:
  REDIS_PASSWORD: "<SET_ME>"
{{- end }}
